<% @newservers.each do | newserver | -%>
newServer({address="<%= newserver['forward_address'] %>", name="<%= newserver['resolver_name'] %>", weight ="<%= newserver['weighting'] %>"}, checkType="A", checkName="hetzner.co.za", maxCheckFailures="3"})
<% end -%>
